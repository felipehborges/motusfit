# Repository agent instructions

For every task in this repository, use the `pc-integration` skill in `.agents/skills/pc-integration/SKILL.md`.

Read `PCS_INTEGRATION.md` before starting work. Before the final response, update it with the current state, completed work, validation results, blockers, and the next action so work can continue safely on another device. Never place secrets in the handoff file.
